# 2026-02-11 Memory Log

## 系統維護與故障排除
- **Discord 延遲問題**：18:22 左右發生 Discord `InteractionEventListener` 延遲（約 7.5 秒），導致 Michael 感覺沒回應。
- **記憶系統修復與決策**：
    - 發現 `qmd` (Quick Memory Daemon) 執行檔遺失 (`ENOENT`)，確認為 Windows 原生環境的路徑/相容性問題。
    - 決定移除 `openclaw.json` 中的 `memory.backend: qmd` 設定，回退至 **Gemini Embedding + SQLite-vec** 模式。
    - 成功重新索引 29 個記憶檔案，系統恢復秒回。
    - GitHub 研究顯示 QMD 在 Windows 上確實存在路徑與依賴性（如誤查 OpenAI Key）等 Issue。
- **ClawDashboard 穩定性挑戰**：
    - 前後端程序出現多次 `Code 1` 崩潰。
    - **修復**：發現前端原先僅監聽 `localhost`，已加上 `--host` 參數改為監聽 `http://192.168.1.186:5173`，以便區域網路連線。
    - 目前以 `v4` 版本維持運行中。

## 規則與排程更新 (HEARTBEAT.md)
- **節能模式**：設定 00:00 - 08:00 間自動跳過 Email 與部落格留言檢查。
- **頻率調整**：部落格留言檢查從每 30 分鐘改為每小時一次。

## 任務成就
- **部落格發布**：發布日誌《關於我今天把「最強大腦」拆掉這件事》，並使用 Nano Banana 生成配圖。
- **Moltbook 摘要**：確認憑證位於 `.config/moltbook/credentials.json`，成功手動執行 `agent-relay-digest` 腳本。

## 遊戲筆記
- **FF14**: 紀錄 G10 藏寶圖（陳舊的羚羊革地圖）採集點於 4.0 區域 70 級節點。

## 用戶回饋
- Michael 詢問為何棄用 `qmd`（因為先前提到它較好）。
- 理由：穩定性優先，且在目前的 workspace 規模下，內建的 Gemini 模式表現優異。
